{
  "name": "hefun-report",
  "version": "1.0.0",
  "description": "Hefun Report",
  "main": "src/main/js/app.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/eyakcn/report"
  },
  "scripts": {
    "build": "mvn -q clean compile && npm install && webpack",
    "serve": "mvn -q clean spring-boot:run",
    "all": "npm install && webpack && mvn -q clean spring-boot:run"
  },
  "dependencies": {
    "babel-core": "^5.8.22",
    "babel-loader": "^5.3.2",
    "jquery": "^2.1.4",
    "react": "^0.14.5",
    "react-dom": "^0.14.5",
    "webpack": "^1.12.9",
    "eslint": "^1.10.3",
    "eslint-loader": "^1.2.0"
  }
}
